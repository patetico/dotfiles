# patetico dotfiles

These are some of the configuration files I use.\
Get started on a new machine using [chezmoi](https://github.com/twpayne/chezmoi):

```bash
chezmoi init patetico --apply --ssh
```
